<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <!--ユーザー情報-->
      <%= render 'userinfo', user: @user %>
      <!--投稿-->
      <%= render 'books/form', book: @book %>
    </div>
    <div class="col-md-8 offset-md-1">
      <!--投稿一覧-->
      <%= render 'books/books_index', books: @user.books %>
    </div>
  </div>
</div>